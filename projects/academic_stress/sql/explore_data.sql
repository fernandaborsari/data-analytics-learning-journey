-- View all data
SELECT *
FROM academic_stress;

-- Count total rows
SELECT COUNT(*) AS total_rows
FROM academic_stress;

-- Check missing values in each column (example)
SELECT
    COUNT(*) AS total_rows
FROM academic_stress;
